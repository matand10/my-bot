

export const BotPreview = (props) => {
    const { bot } = props

    return (
        <section className="bot-preview-container">
            <p>Hello from bot preview</p>
        </section>
    )
}